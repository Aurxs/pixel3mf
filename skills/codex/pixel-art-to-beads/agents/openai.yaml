interface:
  display_name: "像素画转拼豆图纸"
  short_description: "独立生成或读取像素画，交付未压缩与压缩后的原向、镜像四张拼豆图纸"
  default_prompt: "使用 $pixel-art-to-beads，把这张像素画制作成四张拼豆图纸：未压缩原向与镜像、52×52 压缩原向与镜像。"
policy:
  allow_implicit_invocation: true
