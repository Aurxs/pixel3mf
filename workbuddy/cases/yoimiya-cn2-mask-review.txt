Continue the accepted source in output/20260917_182340_yoimiya-e2e-cn2. A second visual review of the source, overlay and component report confirms these Stage 3 decisions:
- Component 5 (26 cells) is true background between the hanging ornament and jaw; remove.
- Component 6 (11 cells) is true background between the neck/shoulder and hanging right hair lock; remove. It is a false-positive foreground despite high model confidence.
- Component 9 is the rope-loop background hole; keep removed.
- Components 2,3,4 are eye whites/highlights; keep. Components 7,8,10,11 are necklace/clothing/bandage whites; keep.
- Keep all actual colored hair, skin, clothing, rope and outline cells. Preserve the continuous baseline. Do not rely on a probability mask that deletes colored silhouette regions.
Use these explicit visual-review decisions to construct a SOURCE-RESOLUTION binary mask for THIS newly generated accepted source. This is mask correction within the authorized complete workflow, not permission to alter source RGB, force a source grid, bypass density or replace the image. Use near-white component selection with reviewed seeds/regions; preserve RGB and record the source hash, removed component decisions and resulting mask hash in an audit JSON. No old mask/source may be used. Place the approved mask and review under output/validation_inputs with distinctive cn2 names, preserving existing files.
Use the wrapper convert --mask-override option to retry the accepted run; preserve the failed manifest via the wrapper's normal failure archiving. Do not use --allow-ambiguous-mask. The approved binary mask should resolve the actual holes rather than acknowledge ambiguity. Complete both 3MF variants and compare the final pixel preview with the accepted source. This review and conversion are authorized; execute directly without more calibration or new image generation.

The interim white-method conversion filled true holes and is not the final deliverable. If the wrapper refuses to modify an already successful run, preserve it and initialize a new direct re-export using EXACTLY this accepted newly generated 01_source.png plus the reviewed mask. Link the original generation run, selected attempt 3, source SHA and mask review in final provenance. This remains the same fresh end-to-end test, with a preserved correction export, not an old-source substitution.
