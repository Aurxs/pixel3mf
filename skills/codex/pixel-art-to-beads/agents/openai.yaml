interface:
  display_name: "像素画转拼豆图纸"
  short_description: "独立生成或读取像素画，匹配拼豆色号，输出正常版、镜像版和用量清单"
  default_prompt: "使用 $pixel-art-to-beads，把这张像素画做成带色号的拼豆图纸，同时输出正常版和左右镜像版。"
policy:
  allow_implicit_invocation: true
